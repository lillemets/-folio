# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the input file is the last one modified
if (!file.exists(outfile) | file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
infile <- asi
infile <- 'rarst'
paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile))
)
file.info('/home/jrl/site/_posts/2017-04-14-evaluating_keyboard_layouts_with_r.md')
file.info(/home/jrl/site/_posts/2017-04-14-evaluating_keyboard_layouts_with_r.md)
file.info('/home/jrl/site/_posts/2017-04-14-evaluating_keyboard_layouts_with_r.md')
file.info('/home/jrl/site/_rmd/evaluating_keyboard_layouts_with_r.Rmd')
2017-04-13 > 2017-04-14
file.info('/home/jrl/site/_rmd/evaluating_keyboard_layouts_with_r.Rmd')$mtime
file.info('/home/jrl/site/_rmd/evaluating_keyboard_layouts_with_r.Rmd')$mtime
file.info('/home/jrl/site/_posts/2017-04-14-evaluating_keyboard_layouts_with_r.md')$mtime
file.info('/home/jrl/site/_rmd/evaluating_keyboard_layouts_with_r.Rmd')$mtime > file.info('/home/jrl/site/_posts/2017-04-14-evaluating_keyboard_layouts_with_r.md')$mtime
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the input file is the last one modified
if (!file.exists(outfile) | file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the input file is the last one modified
if (!file.exists(outfile)) {
KnitPost(infile, outfile)
}
}
file.info('/home/jrl/site/_rmd/evaluating_keyboard_layouts_with_r.Rmd')$mtime > file.info('/home/jrl/site/_posts/2017-04-14-evaluating_keyboard_layouts_with_r.md')$mtime
list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)
basename("_rmd/evaluating_keyboard_layouts_with_r.Rmd")
infile <-  "_rmd/evaluating_keyboard_layouts_with_r.Rmd"
sub(".Rmd$", ".md", basename(infile))
post.dir
list.files(post.dir)
ub(".Rmd$", ".md", basename(infile)) %in% list.files(post.dir)
sub(".Rmd$", ".md", basename(infile)) %in% list.files(post.dir)
grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))
true(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))
grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))
any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
real.outfile = paste0(post.dir, sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist or is older
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))  |
file.info(infile)$mtime > file.info(outfile)$mtime)) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist or is older
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))  |
file.info(infile)$mtime > file.info(outfile)$mtime)) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist or is older
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))  |
file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist or is older
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))) |
file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist or is older
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))) | file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files("_R", pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0("pages/", sub(".Rmd$", ".md", basename(infile)))
# knit only if the input file is the last one modified
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))) |
file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files("_R", pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0("pages/", sub(".Rmd$", ".md", basename(infile)))
# knit only if the input file is the last one modified
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir))) |
file.info(infile)$mtime > file.info(outfile)$mtime) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files("_R", pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0("pages/", sub(".Rmd$", ".md", basename(infile)))
# knit only if the input file is the last one modified
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
#!/usr/bin/Rscript --vanilla
# Compiles all .Rmd files in _R directory into .md files in Pages directory, if the input file is older than the output file.
# This script is a modified version of an example here: http://varianceexplained.org/pages/workflow/
rm(list = ls())
site.dir <- '/home/jrl/site/' # Path to site directory
rmd.dir <- '_rmd' # Path to directory containing rmarkdown files
post.dir <- '_posts/' # Path to posts directory
img.dir <- 'img/' # Path to images directory
setwd(site.dir)
KnitPost <- function(input, outfile, base.url="/") {
require(knitr);
opts_knit$set(base.url = base.url)
fig.path <- paste0(img.dir, sub(".Rmd$", "", basename(input)), "/")
opts_chunk$set(fig.path = fig.path)
opts_chunk$set(fig.cap = "")
render_jekyll(highlight = 'pygments')
knit(input, outfile, envir = parent.frame())
}
for (infile in list.files(rmd.dir, pattern="*.Rmd", full.names=TRUE)) {
outfile = paste0(post.dir, Sys.Date(), "-", sub(".Rmd$", ".md", basename(infile)))
# knit only if the post does not exist
if (!any(grepl(sub(".Rmd$", ".md", basename(infile)), list.files(post.dir)))) {
KnitPost(infile, outfile)
}
}
